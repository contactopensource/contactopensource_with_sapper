describe("Models", () => {

	// beforeEach(() => {        
    //     cy.get("input[name=\"email\"]")
    //     .type("cypress@example.com")
    //     .get("input[name=\"password\"]")
    //     .type("secret");
    //     cy.get('input[type=submit]')
    //     .click()
    // //    cy.visit({
    // //         url: "/signin",
    // //         method: "POST",
    // //         body: {
    // //             email: "cypress@example.com",
    // //             password: "cypress_password"
    // //         }
    // //     });
    // });

	// it("Events → index", () => {
	// 	cy.visit("/events")
	// 	cy.url().should("include", "/events")
	// 	cy.title().should("eq", "Events")
	// 	cy.get("h1").should("eq", "Events")
	// });

	// it("Events → new", () => {
	// 	cy.visit("/events/new")
	// 	cy.url().should("include", "/events/new")
	// 	cy.title().should("eq", "New event")
	// 	cy.get("h1").should("eq", "New event")
	// });

	// it("Goals → index", () => {
	// 	cy.visit("/goals")
	// 	cy.url().should("include", "/goals")
	// 	cy.title().should("eq", "Goals")
	// 	cy.get("h1").should("eq", "Goals")
	// });

	// it("Goals → new", () => {
	// 	cy.visit("/goals/new")
	// 	cy.url().should("include", "/goals/new")
	// 	cy.title().should("eq", "New goal")
	// 	cy.get("h1").should("eq", "New goal")
	// });

	// it("Movies → index", () => {
	// 	cy.visit("/moviews")
	// 	cy.url().should("include", "/movies")
	// 	cy.title().should("eq", "Movies")
	// 	cy.get("h1").should("eq", "Movies")
	// });

	// it("Notes → index", () => {
	// 	cy.visit("/notes")
	// 	cy.url().should("include", "/notes")
	// 	cy.title().should("eq", "Notes")
	// 	cy.get("h1").should("eq", "Notes")
	// });

	// it("Notes → new", () => {
	// 	cy.visit("/notes/new")
	// 	cy.url().should("include", "/notes/new")
	// 	cy.title().should("eq", "New note")
	// 	cy.get("h1").should("eq", "New note")
	// });

	// it("Organizations → index", () => {
	// 	cy.visit("/organizations")
	// 	cy.url().should("include", "/organizations")
	// 	cy.title().should("eq", "Organizations")
	// 	cy.get("h1").should("eq", "Organizations")
	// });

	// it("Organizations → new", () => {
	// 	cy.visit("/organizations/new")
	// 	cy.url().should("include", "/organizations/new")
	// 	cy.title().should("eq", "New organization")
	// 	cy.get("h1").should("eq", "New organization")
	// });

	// it("Persons → index", () => {
	// 	cy.visit("/persons")
	// 	cy.url().should("include", "/persons")
	// 	cy.title().should("eq", "Persons")
	// 	cy.get("h1").should("eq", "Persons")
	// });

	// it("Persons → new", () => {
	// 	cy.visit("/persons/new")
	// 	cy.url().should("include", "/persons/new")
	// 	cy.title().should("eq", "New person")
	// 	cy.get("h1").should("eq", "New person")
	// });

	// it("Places → index", () => {
	// 	cy.visit("/places")
	// 	cy.url().should("include", "/places")
	// 	cy.title().should("eq", "Places")
	// 	cy.get("h1").should("eq", "Places")
	// });

	// it("Places → new", () => {
	// 	cy.visit("/places/new")
	// 	cy.url().should("include", "/places/new")
	// 	cy.title().should("eq", "New place")
	// 	cy.get("h1").should("eq", "New place")
	// });

	// it("Things → index", () => {
	// 	cy.visit("/things")
	// 	cy.url().should("include", "/things")
	// 	cy.title().should("eq", "Things")
	// 	cy.get("h1").should("eq", "Things")
	// });

	// it("Things → new", () => {
	// 	cy.visit("/things/new")
	// 	cy.url().should("include", "/things/new")
	// 	cy.title().should("eq", "New things)
	// 	cy.get("h1").should("eq", "New thing")
	// });

});